
// i: str o: str
/**
 * s '3[a]2[bc]'
 * o aaabcbc 
 * k = 2
 * word: bc
 * pointer 2
 * finalStr = '' + word.repeat(k) + word.repeat(k)
 * k: 3
 * stack [3,a,2,c]
 * word pop c
 * 
 * k: '2'
 * numS [3, 2]
 * wordS ['', a, c]
 * word: a
 * finalStr = c.repeat(2)
 * finalStr = acc
 **/
function decodeStr(str) {
  // number stack
  // word stack
  // k = ''
  // word = ''
  // loop str
  // check char
  // if number add to k
  // if [ 
    // add k to stack reset k
  // if ]
  // add word to stack
    // pop number stack
    // pop word stack
    // use repeat to add to word
  // else
    // add to word
  
}